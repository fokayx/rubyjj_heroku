<h2><%= @exam.name %></h2>
<%= link_to '回上一頁', edit_dashboard_tekmqbs_hospital_path(@exam.hospital) %>
<%= form_for [:dashboard, :tekmqbs, @exam] do |new| %>
  <div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">
      <tbody>
      <% @items.select(:group).uniq.each do |item| %>
        <tr>
          <td style="width:46px">類別</td>
          <td><%= item.group %></td>
        </tr>
        <tr>
          <td>項目</td>
          <td><%= new.collection_check_boxes(:item_ids, Item.where(group:"#{item.group}"), :id, :name) %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  <div class="clearfix form-actions">
    <div class="col-md-offset-3 col-md-9">
      <%= new.submit class:'btn btn-info' %>
    </div>
  </div>
<% end %>
